function Login() {
    return (
        <div className="login">
            <p>
                Ainda não é cadastrado ? {''}
                <a href="http://localhost:3000">Cadastre-se</a>
            </p>
        </div>
    );
}

export default Login;
